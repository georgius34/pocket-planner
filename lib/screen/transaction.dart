import 'package:flutter/material.dart';

class RencanaTabunganScreen extends StatelessWidget {
  const RencanaTabunganScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Transaction"),
    );
  }
}